---
description: blog-admin 路由、权限码与菜单一致性规范
globs: src/{router,store,views}/**/*.{ts,vue}
alwaysApply: false
---

# Route Permission Menu Rules

- 新增页面时必须同时确认：路由注册、菜单配置、权限码映射三项完整。
- 权限控制优先依赖后端返回权限码，不在页面里硬编码角色常量分支。
- 动态路由注入逻辑变更时，必须验证刷新后权限恢复与首次登录场景。
- 菜单隐藏与路由访问策略要分清：隐藏不等于无权限，避免安全误判。
- 兜底路由与 404 页面必须保留，防止权限加载失败后空白页。
- 路由 meta 字段命名保持统一，避免同义字段并存造成判断分裂。
