---
description: blog-admin 与 blog-server 联调约束
globs: src/**/*.{vue,ts}
alwaysApply: false
---

# Admin Joint Debug Rules

- 联调默认后端协议为 `{ code, data, message }`，页面层只消费 `data` 业务体。
- 接口变更前确认管理端页面是否依赖该字段，避免“接口改了表格空白”。
- 401 与 403 场景分开处理：401 走重登，403 给权限不足提示并阻断操作。
- 列表/详情/编辑页共用数据结构时，字段新增优先可选并保持旧字段可用。
- 涉及上传、分页、筛选的接口，联调时逐项核对参数命名与类型。
- 加密接口联调需要同步核对请求体包装与响应解包逻辑。
