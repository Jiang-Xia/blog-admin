---
description: blog-admin 全项目代码注释规范（新功能必注释、复杂逻辑必注释）
alwaysApply: true
---

# 代码注释规范

注释说明**为什么 / 做什么 / 边界**，避免翻译语法。本次新增或修改的代码须补注释；**禁止删除**已有中文注释。

## 新功能 / 新模块（必须）

| 位置 | 写什么 |
|------|--------|
| **新页面/组件** | 菜单归属、主要 CRUD、依赖的后端模块 |
| **表格/表单 composable** | 查询参数、分页约定、弹框提交后是否 refresh |
| **API 封装** | 路径、特殊 query/body、与 C 端是否共用 |
| **权限相关** | 按钮级权限码、为何仅超管可见 |

## 必须加注释的场景

- 鉴权与安全（token、权限、输入校验）
- 异步与竞态（请求序号、取消、loading 锁）
- 生命周期清理（监听、定时器）
- 兼容与迁移（历史字段、降级）
- 表格操作列固定、分页跳转等非显然 UI 约定

## 展示类数据（全项目通则）

- 列表/详情列展示：**优先用后端返回的 label/name**，不在 admin 再维护一份与 `rpg_item_config` 或字典表重复的 map
- 下拉选项来自接口或枚举常量时，在请求处注释数据来源

## 风格

- 一行到两行，放在代码块上方
- 行为变更时注释写清「新行为」与目的
- 禁止废话注释

## 示例

```typescript
// 仅保留最后一次请求结果，避免旧响应覆盖新筛选条件。
let latestRequestId = 0;
```
